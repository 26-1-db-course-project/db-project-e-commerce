# 프론트엔드 환경변수 예시
#
# 개발(localhost)에서는 설정하지 마세요. (비워두면 Vite 프록시가 백엔드로 전달)
#
# 운영(Vercel) 배포 시에만 Vercel 프로젝트 환경변수에 아래를 설정:
#   VITE_API_BASE_URL = https://api.내도메인.com   (AWS 백엔드의 HTTPS 주소)
#
# 주의: Vite 환경변수는 빌드 타임에 주입되므로, 값 변경 후에는 재배포가 필요합니다.
VITE_API_BASE_URL=
